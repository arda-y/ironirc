# non mission critical config, such as timeout, dead switch behavior, etc

MSG_COUNTER = 1  # Increment for each message sent, used in nonce generation
PSK = "supersecretkey"  # Pre-shared key for HKDF and AES-GCM
