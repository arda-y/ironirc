# abstract class for adaptors
# for now it's not abstract, but it will be in the future when we have more adaptors
"""
def send_message(message: str):
    e_msg_chunks = prepare_message_chunks(message)

    # send message here, e.g. via IRC or other protocol
    # for now we'll just print the scrambled message
    transmission = json.dumps(e_msg_chunks)
    print(f"Transmitted (Scrambled): {transmission}")
"""
